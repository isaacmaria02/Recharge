CREATE TABLE USERS (
USERS_PK NUMBER(3) PRIMARY KEY,
NAME VARCHAR2(30),
BALANCE NUMBER(3,2)
)

CREATE TABLE TRANSACTIONS (
TRANS_USER_FK NUMBER(3),
MOBILE NUMBER(10),
AMOUNT NUMBER(3),
OPERATOR VARCHAR2(30)
)

ALTER TABLE TRANSACTIONS
ADD CONSTRAINT FK_TRANSACTIONS
FOREIGN KEY (TRANS_USER_FK) REFERENCES USERS(USERS_PK)
